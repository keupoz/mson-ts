{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "checkJs": true
  },
  "include": ["${configDir}/src", "${configDir}/*.ts", "${configDir}/*.js", "${configDir}/*.mjs", "${configDir}/*.cjs"]
}
